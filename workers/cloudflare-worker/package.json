{
  "name": "wardrobe-picks-spotify-sync",
  "version": "1.0.0",
  "description": "Cloudflare Worker for Spotify Sync",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit",
    "types": "wrangler types"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "wrangler": "^4.87.0"
  }
}
