{
  "ignoreCommand": "git diff --quiet $VERCEL_GIT_PREVIOUS_SHA HEAD . ':(exclude).github' ':(exclude)supabase' ':(exclude)scripts' ':(exclude).agents' ':(exclude).claude' ':(exclude)docs' ':(exclude)workers' ':(exclude)tmp' ':(exclude)scratch' ':(exclude)**.md'",
  "github": {
    "enabled": false
  }
}
