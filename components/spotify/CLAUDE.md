# Spotify Module

Dashboard for Spotify listening history, stats, tags, and music reports. Data comes from R2 JSON shards written by `cloudflare-worker`.

## File Map

```
SpotifyDashboard.tsx              — root: receives all data as props, composes sections
SpotifyLiveNowPlayingSection.tsx  — top section: live now-playing + recent activity
SpotifyLivePlayerPanel.tsx        — now-playing player card
SpotifyWidePlayer.tsx             — expanded player variant

SpotifyRecentlyPlayedSection.tsx  — recently played (day selector + view toggle)
SpotifyRecentlyPlayedDeck.tsx     — deck/card view of recent tracks
RecentlyPlayedViewToggle.tsx      — deck / grid / list view switcher

SpotifyVinylAlbumsPanel.tsx       — vinyl-style album grid (paginated)
SpotifySavedTracksPanel.tsx       — saved tracks list (paginated)
SpotifyTopArtistsPanel.tsx        — top artists (time-range tabs)
SpotifyTopTracksPanel.tsx         — top tracks (time-range tabs)
SpotifyTimeRangeTabs.tsx          — short / medium / long term switcher

SpotifyListeningChart.tsx         — listening activity chart
SpotifyTagCloudCard.tsx           — tag cloud (dynamically imported)
SpotifyTagRadarCard.tsx           — tag radar chart (dynamically imported)
SpotifyTagStreamChart.tsx         — tag evolution stream chart

SpotifyMusicReportSection.tsx     — wraps SpotifyMusicReportBoard with period tabs
SpotifyMusicReportBoard.tsx       — board layout for the music report
SpotifyMusicReportPoster.tsx      — shareable poster view of the report
SpotifyPlaylistDetail.tsx         — single playlist track list

PosterShell.tsx                   — layout wrapper for all share-poster variants
posters/StatsInsightPoster.tsx    — stat insight shareable poster
posters/TopArtistPoster.tsx       — top artist shareable poster
posters/TopTrackPoster.tsx        — top track shareable poster
posters/VibePoster.tsx            — vibe/mood shareable poster

useSpotifyCollectionPagination.ts — "load more" hook for paginated collections
```

## Data Flow

Two layers coexist:

**Server-side (initial load)** — `app/spotify/page.tsx` reads R2 directly via `lib/spotify.ts` using server-only env vars (`R2_SPOTIFY_BUCKET`, `R2_SPOTIFY_PUBLIC_DOMAIN`). Data is passed as props to `SpotifyDashboard`. Page-level `revalidate = 3600`.

**Client-side (pagination / live)** — Paginated panels (`SpotifySavedTracksPanel`, `SpotifyVinylAlbumsPanel`) fetch additional pages from the `cloudflare-worker` public API via `lib/spotify-public-api.ts`. Env var: `NEXT_PUBLIC_SPOTIFY_WORKER_URL`. Now-playing uses `NEXT_PUBLIC_SPOTIFY_NOW_PLAYING_WORKER_URL` (separate lighter worker).

Do not call `lib/spotify.ts` functions from client components — they use server-only R2 env vars.

## Pagination Pattern

`useSpotifyCollectionPagination<T>` is the standard hook for load-more behavior. Takes `initialItems`, `total`, `pageSize`, `fetchUrl`, and `getItemKey`. Fetches `?offset=N&limit=M` against `fetchUrl` (a cloudflare-worker route). Use this hook — do not roll custom pagination.

## Poster System

`PosterShell` provides the outer frame (aspect ratio, background, share controls). The four poster variants in `posters/` implement the content. Each poster receives a specific data subset — they do not fetch anything themselves. Mount them via `SpotifyMusicReportPoster` or the board's share action.

### "Pinned paper" hover lives in `usePosterHover`
The 3D tilt + elastic lift + shadow cross-fade hover (and the resting-rotation init + unmount `killTweensOf` cleanup) is shared via `usePosterHover.ts`. Both `PosterShell` and `SpotifyMusicReportPoster` use it. Note `SpotifyMusicReportPoster` does **not** wrap itself in `PosterShell` — it has its own pin/tape markup (`SpotifyMusicReportBoard.module.css`) and only shares the hover hook. Editing the hook changes the feel of every poster; pass `restRotation` per card.

## Architecture Decisions

### Tag components are dynamically imported
`SpotifyTagCloudCard` and `SpotifyTagRadarCard` are dynamically imported in `SpotifyDashboard` because they load D3 and tag data. Do not make them static imports.

### Music report fallback
`SpotifyMusicReportSection` renders an error message if `report` is null — it does **not** fall back to local aggregation. The `report.json` must be generated by the worker's full-sync run. If the report is missing, wait for the next cron cycle or trigger a manual full sync.

### Time-range tabs are local state
`SpotifyTimeRangeTabs` controls which time-range view is shown (short/medium/long). All three ranges are passed as props from the server; switching is purely client-side — no refetch.

## Hard Constraints

- All `lib/spotify.ts` exports are server-only (R2 env vars). Import only from server components or `page.tsx`.
- All cloudflare-worker fetch URLs must go through `getSpotifyPublicApiUrl()` from `lib/spotify-public-api.ts` — do not hardcode worker URLs.
- `SpotifyDashboard` itself is a client component (it holds state for view modes and time ranges). Keep data loading in `app/spotify/page.tsx` server components and pass down as props.
